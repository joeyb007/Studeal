venv
.env
CLAUDE.md

# Python build artifacts
*.egg-info/
__pycache__/
*.pyc
.pytest_cache/

# macOS
.DS_Store

# docs/: ignore all by default (regenerable dumps, plans, superseded history);
# whitelist only the eval results — the evidence behind the numbers.
docs/**
!docs/**/
!docs/evals/results.md
!docs/evals/results.jsonl

# Auth state — contains FB session cookies; NEVER commit
tests/evals/_fb_state.json

# Agent run traces — page snapshots + full LLM prompts; local only
traces/
/media/
celerybeat-schedule*
